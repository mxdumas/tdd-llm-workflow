**Updating state in Jira:**

Use the MCP Jira server to update issue status:

```
Use the Jira MCP tool to:
1. Transition story to "Done" status
2. Add comment with task summary
3. Update epic progress if all stories complete
```

Also update `.tdd-state.local.json` to reset local session state.